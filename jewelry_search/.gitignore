# Python
__pycache__/
*.pyc
.venv/
venv/

# NOTE: data/ (index.faiss, metadata.json, embeddings.npy, config.json) is
# intentionally committed so the deployed app has a prebuilt index to load.
# Regenerate it locally with: python prepare_data.py
